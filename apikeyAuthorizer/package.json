{
  "name": "apikey-authorizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-build": "npm run-script test && npm run-script build",
    "build": "npm prune --production && rm -f ./function.zip && zip -r ./function.zip . -x './src/test/*' -x '*.md'  -x '*.gitignore'",
    "test": "npm install && mocha './src/test/**/*.test.js' --recursive"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "aws-sdk": "^2.1060.0",
    "aws-sdk-mock": "^5.6.2",
    "mocha": "^9.2.0",
    "chai": "^4.3.6",
    "lambda-tester": "^4.0.1",
    "proxyquire": "^2.1.3"
  }
}
