{
  "name": "weblogout",
  "version": "1.0.0",
  "description": "Lambda function that handles user logout by invalidating jwt token.",
  "main": "index.js",
  "scripts": {
    "build": "npm run prune && npm run zip",
    "prune": "(cd ../../authCommon && npm --silent prune --production) && npm --silent prune --production",
    "zip": "rm -f ./function.zip && zip -q -r ./function.zip .  -x './src/test/*' -x '*.gitignore' -x 'coverage/*' -x '.nyc_output/*' -x '.env*'",
    "test-build": "(cd ../../authCommon && npm install) && npm run test && npm run coverage && npm run build",
    "coverage": "jest --coverage",
    "test": "jest --coverage --runInBand --silent"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.15.0",
    "bunyan": "^1.8.15",
    "jsonwebtoken": "^9.0.2",
    "pn-auth-common": "file:../authCommon"
  },
  "devDependencies": {
    "dotenv": "^16.5.0",
    "jest": "^30.0.2",
    "nyc": "^17.1.0"
  }
}
